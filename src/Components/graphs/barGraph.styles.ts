import { StyleSheet } from 'react-native'
import { theme } from '../../theme'

export const barGraphStyles = StyleSheet.create({
    victoryChart: {
        paddingLeft: 70,
        paddingBottom: 30,
        paddingRight: 50,
        paddingTop: 30,
    },
})
